import 'package:flutter/material.dart';

class todo2Screen extends StatelessWidget {
  const todo2Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
